import XCTest
@testable import NnSwiftUIHelpers

final class NnSwiftUIHelpersTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NnSwiftUIHelpers().text, "Hello, World!")
    }
}
